import { FavoritousuarioDirective } from './favoritousuario.directive';

describe('FavoritousuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritousuarioDirective();
    expect(directive).toBeTruthy();
  });
});
